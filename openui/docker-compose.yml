version: "3.9"

services:
  openui:
    image: node:20-alpine
    container_name: openui
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm ci && npm run dev"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true